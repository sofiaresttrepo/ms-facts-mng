apiVersion: v1
kind: Service
metadata:
  name: fact-mng-mongo
spec:
  ports:
  - port: 27017
    name: fact-mng-mongo
  selector:
    app: fact-mng-mongo
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fact-mng-mongo-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: standard-exp  
---
apiVersion: apps/v1beta2 # for versions before 1.9.0 use apps/v1beta2
kind: Deployment
metadata:
  name: fact-mng-mongo
spec:
  selector:
    matchLabels:
      app: fact-mng-mongo
  strategy:
    type: Recreate      
  template:
    metadata:
      labels:
        app: fact-mng-mongo
    spec:
      containers:
      - image: mongo:4
        name: fact-mng-mongo        
        ports:
        - containerPort: 27017
          name: fact-mng-mongo
        volumeMounts:
        - name: fact-mng-mongo-persistent-storage
          mountPath: /data/db
        resources:
          requests:
            cpu: "50m"
      volumes:
      - name: fact-mng-mongo-persistent-storage
        persistentVolumeClaim:
          claimName: fact-mng-mongo-pv-claim      